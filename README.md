# MedaStare (Next.js)

Bu proje statik HTML yapısından Next.js yapısına taşındı.

## Geliştirme

```bash
npm install
npm run dev
```

Uygulama açıldıktan sonra `/` rotası otomatik olarak `public/index.html` sayfasına yönlenir.
